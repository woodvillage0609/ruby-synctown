

<table style="position:relative; margin:0px;" >

<tr>

<th style="font-size:9px;">
 <ul>
 	<li style="position:absolute; top:10px; left:5px; text-align:center;">
 	<%= image_tag(post.user.image, {class:"img-circle"}) %><br>
	<% if user_signed_in? && current_user?(post.user) %>
	<%= link_to 'Destroy', [post.note, post],
	               method: :delete,
	               data: { confirm: 'Are you sure?' } %>
	<% end %>
 	</li>
 </ul>
</th>

<td style="padding:10px 0px 0px 70px; width:100%;">
	<ul>
	 <li style="padding-bottom:10px; font-size:20px;color:#000080;"><%= post.user.name %></li>
	 <li style="font-size:15px; padding-bottom:20px;"><%= post.body %></li>
    </ul>
</td>

</tr>

</table>